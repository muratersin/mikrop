const login = require('./login');
const register = require('./register');
const loginFormValidation = require('./login-form-validation');
const registerFormValidation = require('./register-form-validation');

module.exports = {
  login,
  register,
  loginFormValidation,
  registerFormValidation,
};
